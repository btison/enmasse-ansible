{
    "apiVersion": "v1",
    "kind": "Secret",
    "metadata": {
        "name": "{{ tls_secret_name }}"
    },
    "type": "kubernetes.io/tls",
    "data": {
        "tls.key": "{{ tls_key }}",
        "tls.crt": "{{ tls_crt }}"
    }
}